/**
 * Where this app's lesson recordings are published, relative to the page, or
 * null when it has none. ORBIT's lessons are far too many to record (well over
 * a hundred hours), and it runs as a desktop app with the memory to make the
 * voice itself, so it reads every lesson on the device.
 */
export const RECORDINGS_URL: string | null = null
